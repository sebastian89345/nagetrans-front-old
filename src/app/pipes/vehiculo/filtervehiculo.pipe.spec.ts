import { FiltervehiculoPipe } from './filtervehiculo.pipe';

describe('FiltervehiculoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltervehiculoPipe();
    expect(pipe).toBeTruthy();
  });
});
