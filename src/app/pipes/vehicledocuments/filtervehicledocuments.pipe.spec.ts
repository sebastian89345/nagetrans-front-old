import { FiltervehicledocumentsPipe } from './filtervehicledocuments.pipe';

describe('FiltervehicledocumentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltervehicledocumentsPipe();
    expect(pipe).toBeTruthy();
  });
});
