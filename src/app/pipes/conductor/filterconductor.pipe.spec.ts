import { FilterconductorPipe } from './filterconductor.pipe';

describe('FilterconductorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterconductorPipe();
    expect(pipe).toBeTruthy();
  });
});
